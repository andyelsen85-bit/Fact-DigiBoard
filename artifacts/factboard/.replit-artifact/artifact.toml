kind = "web"
previewPath = "/"
title = "DigiBoard"
version = "1.0.0"
id = "artifacts/factboard"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 18576

[services.development]
run = "pnpm --filter @workspace/factboard run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/factboard", "run", "build" ]
publicDir = "artifacts/factboard/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "18576"
BASE_PATH = "/"
